# 🔧 Obsidian Templater 自动化配置指南

## 📋 第1步：安装Templater插件

1. 打开Obsidian
2. 进入 `设置` → `社区插件`
3. 关闭 `安全模式`
4. 点击 `浏览`，搜索 `Templater`
5. 点击 `安装`，然后 `启用`

## 📋 第2步：配置Templater

### 基本设置：
1. 进入 `设置` → `Templater`
2. **Template folder location**: 设置为 `templates`
3. **Trigger Templater on new file creation**: 开启
4. **Enable System Commands**: 开启
5. **Allow automatic script execution**: 开启

### 脚本设置：
1. 在你的Obsidian库根目录创建 `scripts` 文件夹
2. 将以下文件复制过去：
   - `/Users/wygg/community-observation-web/scripts/auto_image_handler.py`
   - `/Users/wygg/community-observation-web/scripts/obsidian_user_functions.js`

### 用户脚本设置：
1. 在Templater设置中找到 `User Scripts` 部分
2. 点击 `Add new script`
3. 脚本名称：`user_functions`
4. 脚本内容：复制 `obsidian_user_functions.js` 的内容

## 📋 第3步：设置模板

1. 将 `simple-auto-template.md` 复制到你的Obsidian库的 `templates` 文件夹
2. 在Templater设置中，确保模板文件夹指向正确的位置

## 📋 第4步：设置快捷键

1. 进入 `设置` → `快捷键`
2. 搜索 "Templater: Insert Template"
3. 找到你的模板文件
4. 设置快捷键为 `Cmd+Shift+O`

## 🎯 使用流程

### 创建新文章：
1. 按 `Cmd+Shift+O`
2. 输入中文标题（如："社区快递柜观察"）
3. 自动创建文件，包含：
   - 英文文件名：`2026-02-10-express-cabinet.md`
   - 中文内容模板
   - 图片处理命令

### 处理图片：
1. 拍完照片，放到桌面
2. 复制文章中的Python命令到终端运行
3. 图片自动：
   - 重命名为英文
   - 移动到正确目录
   - 更新文章中的图片路径

## 📋 测试步骤

1. **测试模板创建**：
   ```bash
   # 在Obsidian中按 Cmd+Shift+O
   # 输入："垃圾桶观察"
   # 应该创建：2026-02-10-trash-bin.md
   ```

2. **测试图片处理**：
   ```bash
   # 将测试图片放到桌面
   # 运行文章中的Python命令
   # 检查：public/images/observations/ 目录
   ```

3. **测试网站显示**：
   ```bash
   # 重启PM2：pm2 restart community-observation-web
   # 访问：http://localhost:3000
   # 检查图片是否正常显示
   ```

## 🚨 常见问题

### Q: 提示"tp.user.getInput is not defined"
A: 确保用户脚本已正确配置在Templater设置中

### Q: Python脚本无法运行
A: 检查Python3是否安装：`python3 --version`
   检查文件权限：`chmod +x scripts/auto_image_handler.py`

### Q: 图片处理失败
A: 确保图片在桌面，且格式为.jpg/.png

### Q: 网站上图片404
A: 检查图片是否在 `public/images/observations/` 目录中
   检查文章中的图片路径是否正确

## 🎉 完成后的效果

- 🖱️ 一键创建文章（Cmd+Shift+O）
- 🤖 智能文件名翻译
- 🖼️ 自动图片处理
- 🌏 中文内容保留
- 🔧 技术问题解决

你就拥有了一个完全自动化的考现学笔记系统！